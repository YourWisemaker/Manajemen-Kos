/**
 * Dashboard placeholder — Task 8.4
 * --------------------------------
 * Minimal stub so the (dashboard) route group renders within `AppShell`.
 * The full multi-property dashboard is built in Task 12.
 */
export default function DasborPage() {
  return (
    <section className="flex flex-col gap-2">
      <h1>Dasbor</h1>
      <p className="text-muted-foreground">
        Ringkasan multi-properti akan tampil di sini.
      </p>
    </section>
  );
}
