/**
 * Onboarding placeholder — Task 8.4
 * ---------------------------------
 * Minimal stub so the (onboarding) route group renders within `WizardShell`.
 * The full five-step wizard is built in Task 11.
 */
export default function OnboardingPage() {
  return (
    <section className="flex flex-col gap-2">
      <h1>Selamat datang</h1>
      <p className="text-muted-foreground">
        Wizard onboarding lima langkah akan tampil di sini.
      </p>
    </section>
  );
}
