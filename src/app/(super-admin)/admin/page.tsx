/**
 * Super-admin placeholder — Task 8.4
 * ----------------------------------
 * Minimal stub so the (super-admin) route group renders within `AdminShell`.
 * The full platform console is built in Task 21.
 */
export default function AdminPage() {
  return (
    <section className="flex flex-col gap-2">
      <h1>Ringkasan Platform</h1>
      <p className="text-muted-foreground">
        Metrik platform dan daftar tenant akan tampil di sini.
      </p>
    </section>
  );
}
